"Rendering" in react refers to the function that defines that component being run

When will a react component rerender?
- It receives new props
- internal state values change (calling the setState function)