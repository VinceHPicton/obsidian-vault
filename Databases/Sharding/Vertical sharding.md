Vertical sharding is about separating concerns across different DBs or schemas, eg user profiles DB and a transactions DB, as opposed to [[Horizontal sharding]] which separates rows within a column.

In practice large apps will use both horizontal and vertical