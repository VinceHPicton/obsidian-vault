In PostgreSQL, **ownership means ultimate control over an object** (database, schema, table, etc.) by a specific role: the owner implicitly has all privileges on that object, can modify or drop it, grant permissions to others, and even transfer ownership

Think: Owner = root user for that database/schema/object