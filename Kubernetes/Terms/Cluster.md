A group of worker nodes that can communicate, it contains 1 **master node** and N **worker nodes**, the nodes share resources.

The key point is a cluster lets you treat a group of machines as 1 logical platform, via the master.