[[CDN]]

Content delivery(/distribution) network.

These are for sites serving large amounts of static media.

User requests first go to a CDN server (an edge server), CDN serves it if it has it, else it queries it from backend servers and caches the result for future requests.

Examples
[[AWS/CloudFront/CloudFront|CloudFront]]
[[Cloudflare]]
