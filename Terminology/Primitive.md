A _primitive_ is a basic building block provided by a language or runtime that you use to construct more complex behaviour.

A primitive is something the language gives you directly, with behaviour defined by the language/runtime specification, and which other abstractions are built on top of.