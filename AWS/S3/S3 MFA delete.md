An extra protection, that can be enabled/disabled only by the root account (bucket owner), which means you need an MFA code to:
- The permanent deletion of object versions
- Suspending versioning

Only need a code for destructive actions, so wouldnt need one to enable versioning for example.
